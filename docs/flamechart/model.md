# FlameChart model

```yaml
kind: "FlameChart"
spec:
  palette: <enum = "package-name" | "value">
  showSettings: <boolean>
  showSeries: <boolean>
  showTable: <boolean>
  showFlameGraph: <boolean>
  traceHeight: <number> # Optional, must be >= 0
```
